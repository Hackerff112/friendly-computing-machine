name: SSH-VPS-Tmate
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Tmate Session
      uses: mxschmitt/action-tmate@v3
      
